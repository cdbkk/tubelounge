## What

<!-- One line: what does this change do? -->

## Why

<!-- What problem does it solve or what does it improve? -->

## Checks

- [ ] `python -m pytest -q` passes
- [ ] `node --check static/app.js` and `node --check extension/background.js` pass
- [ ] `manifest.json` is valid JSON
- [ ] No new dependencies (or justified below)

## Notes

<!-- Anything a reviewer should know. Delete if empty. -->
